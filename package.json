{
  "name": "home",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Collects readings from home sensors and drives the HRV unit based on air quality.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/main.ts",
    "test": "node --test \"tests/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
